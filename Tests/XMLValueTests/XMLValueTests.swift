    import XCTest
    @testable import XMLValue

    final class XMLValueTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
//            XCTAssertEqual(XMLValue().text, "Hello, World!")
        }
    }
